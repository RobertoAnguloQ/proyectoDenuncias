<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-files-path
        name="camera_images"
        path="Pictures/ProyectoDenuncias/" />
    <cache-path
        name="camera_cache"
        path="temp_photos/" />
    <!-- Directorio para fotos de la cámara -->
    <external-files-path
        name="denuncias_images"
        path="Pictures/ProyectoDenuncias/" />

    <!-- Directorio para documentos PDF generados -->
    <external-files-path
        name="denuncias_documents"
        path="Documents/Denuncias/" />

    <!-- Directorio temporal para procesamiento de imágenes -->
    <cache-path
        name="denuncias_temp"
        path="temp_photos/" />

    <!-- Directorio para archivos descargados -->
    <external-files-path
        name="denuncias_downloads"
        path="Downloads/" />

    <!-- Directorio para archivos compartidos temporalmente -->
    <external-cache-path
        name="denuncias_shared"
        path="shared/" />

    <!-- Directorio raíz de caché (usar con precaución) -->
    <cache-path
        name="denuncias_cache_root"
        path="/" />
</paths>